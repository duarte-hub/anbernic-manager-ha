# Only needed for "local path" mode with docker-compose: the folder on
# the Docker host (already containing your ROMs, or an SD card / SMB
# share already mounted there) to bind into the container.
ROMS_HOST_PATH=/path/to/your/anbernic/share
